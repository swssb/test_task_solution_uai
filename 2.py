# Будет ошибка, при попытке принта переменной b, так как в локальной области видимости
# переменная b еще не определена, можно объявить переменную b внутри функции с приставкой global,
# тогда код сработает

